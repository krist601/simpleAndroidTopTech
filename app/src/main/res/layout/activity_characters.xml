<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/listView"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:layout_marginTop="0dp"
            android:background="@color/white"
            android:layout_gravity="center"
            tools:listitem="@layout/cell_character" />
        <Button
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="@string/view_general_reload_string"
            android:background="@color/white"
            android:id="@+id/reloadButton"
            android:visibility="gone"/>
        <ProgressBar
            android:id="@+id/progressBar"
            style="?android:attr/progressBarStyleLarge"
            android:layout_centerInParent="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:visibility="gone"/>
        <TextView
            android:textSize="@dimen/text_size_normal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/view_general_news_no_data_string"
            android:id="@+id/noDataTextView"
            android:layout_centerInParent="true"
            android:visibility="gone"/>
        <ImageView
            android:contentDescription="@string/general_image_description"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:scaleType="fitXY"
            android:src="@drawable/graphic_element_general_top_shadow"/>
    </RelativeLayout>

</layout>